{
  "version": "0.2.0",
  "configurations": [
      {
          "type": "mix_task",
          "name": "Run Issue",
          "request": "launch",
          "task": "run",
          "exitAfterTaskReturns": false,
          "projectDir": "${workspaceRoot}",
      }
  ]
}
